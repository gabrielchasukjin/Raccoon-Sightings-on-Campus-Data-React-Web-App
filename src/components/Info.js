import React from 'react'

function Info(props) {
    return(
        <div className="contact-card"> 
            <h4 className="card-location">{props.title}</h4>
        </div>
    )
}

export default Info
